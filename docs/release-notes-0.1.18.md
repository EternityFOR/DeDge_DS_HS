# DeDge DeepSeek Harness v0.1.18 test build

- Earlier-history pages are inserted above the current conversation and remain recursively collapsed.
- The history loader locks immediately and shows an animated spinner until completion.
- Mid-turn user messages use a smaller `YOU · STEER` card distinct from the initial user prompt.
- Harness RC revisions sharing the same protocol base are compatible, avoiding rc.6/rc.7 upgrade lockouts.

Bundled DeepSeek Harness: `0.1.0-rc.7`.
