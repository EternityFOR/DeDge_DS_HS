# DeDge DeepSeek Harness v0.1.23 test build

- Search is now a floating overlay over the conversation, matching the VS Code search surface.
- Typing only updates highlights and match count; navigation happens on explicit previous/next clicks.
- Navigation expands the target task/detail and scrolls the active result into view.
- Opening search uses the current text selection when available.

Bundled DeepSeek Harness: `0.1.0-rc.7`.
