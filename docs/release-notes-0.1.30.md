# DeDge DeepSeek Harness v0.1.30 test build

- Search navigation now uses Chromium CSS Custom Highlight `Range` objects for precise target-word highlighting without rewriting message DOM.
- Target task expansion and scrolling remain unchanged, without whole-task tinting or long-message DOM stalls.

Bundled DeepSeek Harness: `0.1.0-rc.7`.
